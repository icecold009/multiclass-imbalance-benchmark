[tool.pytest.ini_options]
addopts = "-q --basetemp=artifacts/test-tmp"
cache_dir = "artifacts/test-cache"
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
exclude = [".venv", "data/raw", "data/processed", "results", "artifacts"]
